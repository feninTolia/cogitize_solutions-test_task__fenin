import { Tabs } from '@/shared/lib/constants';
import React from 'react';

const HierarchyTab = () => {
  return (
    <div className=" h-full flex justify-center items-center">
      <span className=" text-3xl">{Tabs.Hierarchy}</span>
    </div>
  );
};

export default HierarchyTab;
